apiVersion: v1
kind: Service
metadata:
  name: {{ include "minikb.fullname" . }}
  labels:
    {{- include "minikb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "minikb.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
